package aoc

/** Dummy implementation to make Main compile for native.
  *
  * Day25 does not compile with/for native. The error message is ...
  * {{{
  * 'init_scala.collection.mutable.ArrayBuffer' defined with type 'void (i8*, i8*)*'
  * }}}
  * ... and I have not figured out (yet) what is wrong.
  */
object Day25 {

  val input = List.empty[String]

  object Part1 {
    def solve(input: List[String]): (Int, Long) = {
      (0, 0L)
    }
  }

  object Part2 {
    def solve(input: List[String]): (Int, Long) = {
      (0, 0L)
    }
  }
}
