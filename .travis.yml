sudo: required

language: scala

notifications:
  email:
    recipients:
      - roland@tritsch.org
    on_success: always
    on_failure: always

before_install:
  - bash bin/travis_setup.sh

script:
  #- sbt "aocJVM/testOnly aoc.Day*Spec -- -n aoc.BuildTest"
  #- sbt "aocJVM/testOnly aoc.Day*Spec -- -n aoc.SolutionTest"
  #- sbt "aocJVM/testOnly aoc.Day*Spec -- -l aoc.SlowTest"
  #- sbt "aocJVM/testOnly aoc.Day*Meter"
  #- sbt "aocJVM/testOnly aoc.Day*Check"
  #- travis_wait 60 sbt aocJVM/test
  #- travis_wait 30 sbt aocJVM/run
  - travis_wait 60 sbt aocNative/run

addons:
  apt:
    update: true
